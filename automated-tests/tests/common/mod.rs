// since all thhe files in the tests folder will be crates
// you can create a common file that can contain the shared code between the crates
// files in subdirectories in the tests folder do not get compiled as crates
pub fn setup() {}
